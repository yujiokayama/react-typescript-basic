import React from "react";

const Top: React.FC = () => {
  const testFunc = (arg: string): string => {
    return arg;
  };

  return (
    <div>
      <h1>Top Page</h1>
    </div>
  );
};

export default Top;
